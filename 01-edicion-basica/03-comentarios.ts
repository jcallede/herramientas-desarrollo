/*
    Objetivo:
        Comentar varias líneas de código, sólo dejar un bloque comentado.

    Tips:
        ⌘ + ç
        Ctrl + ç
*/

const a = 10;
const b = 20;
const c = { a, b };


const a = 30;
const b = 40;
const c = { a, b };


console.log( a, b, c );
